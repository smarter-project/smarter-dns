#

application:
  namespace: smarter
  appName: smarter-dns
  configMapCoreName: smarter-dns-core
  configMapConfigName: smarter-dns-config

image:
  repository: registry.gitlab.com/smarter-project/smarter-dns
  #repository: registry.gitlab.com/smarter-project/smarter-dns
  # @default -- chart.appVersion
  tag: ""
  pullPolicy: IfNotPresent

configuration:
  nodeSelector:
    smarter-dns: enabled
